# BigQuery MCP Server Configuration

# Primary method: Path to service account key file
# The project ID will be automatically read from this file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Alternative: Set project ID manually (only needed if not using service account file)
# GOOGLE_CLOUD_PROJECT=your-project-id
# BQ_PROJECT_ID=your-project-id

# Optional: BigQuery location/region (default: US)
BQ_LOCATION=US

# Development settings
NODE_ENV=development 